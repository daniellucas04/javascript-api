const express = require('express');

const router = express.Router();

/**
 * Retorna um array com todas as tasks do banco
 */
// router.get('/tasks', (request, response) => {
//     response.status(200).send('Router working')
// })

module.exports = router;